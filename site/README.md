# SightLoop Site

This directory contains the documentation site for SightLoop.

Useful commands:

```bash
npm install
npm start
npm run build
```

The site renders the repository documentation from `../docs` and is deployed with the GitHub Actions workflow in `../.github/workflows/deploy-docs.yml`.
