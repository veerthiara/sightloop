session_name: dev-session
environment: dev

camera:
  source: 0
  width: 1280
  height: 720
  fps: 30

output:
  frames_dir: artifacts/frames
  clips_dir: artifacts/clips
  logs_dir: artifacts/logs

debug:
  save_frame_interval_secs: 5.0
  display_enabled: true
  metrics_log_interval_secs: 10.0
