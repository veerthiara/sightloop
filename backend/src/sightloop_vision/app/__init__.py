"""App runner — placeholder entry point for the SightLoop vision service."""


def main() -> None:  # pragma: no cover
    """Application entry point. Will be wired to the camera pipeline in rev-05."""
    print("SightLoop Vision — starting (config-driven runner coming in rev-05)")
