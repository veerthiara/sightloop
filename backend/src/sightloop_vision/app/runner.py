"""App runner — entry point wired to the CLI script.

Will be expanded in rev-05 (main camera pipeline) to accept a config path,
load AppConfig, initialise the camera source, and start the processing loop.
"""


def main() -> None:  # pragma: no cover
    """Application entry point."""
    print("SightLoop Vision — starting (full runner in rev-05)")
